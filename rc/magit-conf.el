;;; magit-conf.el --- Summary

;;; Commentary:
;; Settings only for magit

;;; Code:

(global-set-key (kbd "C-x g") 'magit-status)
(global-set-key (kbd "C-x M-g") 'magit-dispatch-popup)


(provide 'magit-conf.el)
;;; magit-conf.el ends here
